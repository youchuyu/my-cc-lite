---
description: "Execute the next pending my-cc-lite plan item"
argument-hint: "[item id]"
---

# my-cc-lite do

Dispatch to the bundled `do` skill.

1. Read `skills/do/SKILL.md` from the active plugin root.
2. Follow that skill exactly.
3. Treat the user's arguments as:

```text
$ARGUMENTS
```
