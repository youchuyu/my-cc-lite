---
description: "Verify a my-cc-lite run and record evidence"
---

# my-cc-lite verify

Dispatch to the bundled `verify` skill.

1. Read `skills/verify/SKILL.md` from the active plugin root.
2. Follow that skill exactly.
3. Treat the user's arguments as:

```text
$ARGUMENTS
```
