---
description: "Create or update a my-cc-lite state-backed plan"
argument-hint: "<task description>"
---

# my-cc-lite plan

Dispatch to the bundled `plan` skill.

1. Read `skills/plan/SKILL.md` from the active plugin root.
2. Follow that skill exactly.
3. Treat the user's arguments as:

```text
$ARGUMENTS
```
