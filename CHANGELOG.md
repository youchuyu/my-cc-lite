# Changelog

## 0.1.0

- Initial standalone `my-cc-lite` plugin project.
- Includes state-backed `/plan`, `/do`, `/verify`, and `/status` workflow.
- Includes lifecycle hooks, local state helper, state contract, and companion-plugin compatibility contract.
